# ============================================================
# A股量化交易系统 - Python 依赖清单
# Python 3.11+ 要求
# 安装: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# ============================================================

# --- 科学计算（严格锁定，防止计算逻辑微差导致回测结果不一致）---
pandas==2.2.3
numpy==1.26.4
pyarrow==15.0.2
scipy==1.13.1                  # pandas_ta 隐式依赖，显式锁定避免冲突
pandas_ta==0.3.14b0
optuna==3.6.1

# --- 数据源：下限锁定 + 上限防 breaking change ---
akshare>=1.12.0,<2.0.0
baostock>=0.8.8,<1.0.0
tushare>=1.4.0,<2.0.0

# --- 可视化（回测净值曲线、回撤图、绩效报告）---
matplotlib==3.9.2

# --- 工具库 ---
pyyaml==6.0.2
python-dotenv==1.0.1
loguru==0.7.3
typer==0.12.3
tqdm==4.66.5                   # 数据下载进度条

# --- 测试 ---
pytest==8.3.2
pytest-cov==5.0.0              # 测试覆盖率（CI/CD 用）